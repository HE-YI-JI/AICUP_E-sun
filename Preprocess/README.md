# Description of File
This script generates normal transaction-based features and merges them with`tgn_output.csv` to produce the final feature dataset `final_data.csv`.

## File Included
- NormalFeat.py

## How to Use
Run `NormalFeat.py`.  
Ensure the required Parquet file `acct_alert.parquet` exists in the correct location.