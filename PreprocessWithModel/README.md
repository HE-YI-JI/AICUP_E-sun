# Description of File
This script generates temporal graph features using a TGN model and saves the results as `tgn_output.csv`, which will later be merged into the final feature set.

## File Included
- TGN.py

## How to Use
Run `TGN.py`.  
Ensure the required Parquet file `acct_alert.parquet` is available in the correct directory.