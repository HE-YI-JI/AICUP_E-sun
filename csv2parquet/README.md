# Description of File
This script converts a CSV file into a Parquet file for faster reading and efficient storage.

## File Included
- csv2parquet.py

## How to Use
Run `csv2parquet.py` directly.  
Ensure the target CSV file is placed in the correct directory.